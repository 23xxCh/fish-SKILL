# 闲鱼商品采集与定时推送设计

## 产品边界

应用只在用户的 Windows 电脑上运行。浏览器交互使用 Playwright 和本机 Edge，用户负责扫码登录、验证码和风险控制；程序只读取公开可见的搜索结果，不下载图片，也不自动向卖家发送任何消息。

## 两条工作流

### 单次采集

`PySide6 -> CrawlWorker -> 共用登录目录的 Edge -> 搜索卡片 -> ProductRecord -> 检查点 -> openpyxl -> WPS Excel`

保留原有暂停、继续、停止、异常导出和检查点恢复能力。

### 定时采集与飞书推送

`ScheduledCollectionConfig -> QTimer -> CrawlWorker -> 专用 Edge -> ProductRecord -> Excel -> FeishuProvider`

- 只保存一份当前采集规则：关键词、上限、输出目录和筛选条件。
- 用户点击启动后立即执行第一轮；每轮完成后才计算下一次，避免同一专用 Edge 配置并发使用。
- 每轮完整导出 Excel，飞书消息展示真实总数与最多 10 条 HTTPS 商品链接。
- 停止定时采集会取消后续轮次；正在进行的一轮仍会安全导出，但不再推送飞书。
- 程序重启后会恢复已启用的规则，并在设定间隔后执行；关闭主窗口时缩到系统托盘，电脑仍需保持开机、联网且不休眠。

### 定时任务健康、可靠投递与降噪

`定时采集结果 -> 本机 outbox -> 飞书发送 -> 成功/重试状态 -> 任务健康卡`

- 每轮推送先持久化到 SQLite；失败按 5、30、120 秒重试，程序重启后继续未完成的定时采集投递。
- “仅推送新增/变化”默认关闭，开启后的首轮完整推送并建立基线；后续仅推送新的商品 ID 或价格变化。无变化时仍导出 Excel，但不打扰飞书。
- 右侧任务健康卡展示最近成功时间、结果数、最近飞书投递状态和待重试数量。状态只保存必要的时间、数量、导出路径与错误摘要，不保存图片字节或凭证。

### 视频商品封面与白图降级

`搜索卡片 video poster -> ProductRecord.image_url -> 图片有效性检查 -> 飞书图片卡片或文字卡片`

- 视频商品优先提取可见 `video` 的 HTTPS `poster` / 封面地址，再回退普通图片与懒加载图片属性。
- 推送前只上传边长至少 32 像素、非全白、非全透明且不超过 1,600 万像素的图像；视频封面缺失或占位图无效时，保留商品标题、价格和 HTTPS “查看商品”按钮，不发送空白图片区。
- 商品 HTTPS 链接是通用网页兜底链接。仅凭商品 ID 没有经过验证的官方闲鱼 App 唤起地址，因此当前不构造非官方 URI scheme；需要一键唤起 App 时，必须另行验证官方分享链接或部署受控 HTTPS 跳转页。

## 核心对象

- `SearchFilters`：价格、地区、发布时间、四个商品条件和排序。
- `ScheduledCollectionConfig`：当前 `CrawlConfig`、5/10/15/30/60 分钟间隔和启用状态。
- `ProductRecord`：卡片字段、真实聊天链接和跨页去重信息。
- `NotificationBatch`：定时任务名、最多 10 件展示商品、真实总数和商品标签。
- `FeishuProvider`：本机凭据校验、测试消息和结果卡片发送。

## SQLite

数据库位置为 `%LOCALAPPDATA%\GoofishLinkCollector\monitor.db`：

- `settings`：加密后的飞书凭据、绑定用户和一份定时采集规则。

旧版监控的 `tasks`、`products`、`scans` 和 `outbox` 表会保留，防止升级时删除用户数据；新版不显示、调度或读取它们。

## 飞书绑定

飞书官方 `lark-oapi` 长连接在独立子进程中运行，父线程提供 5 分钟绑定窗口。只有文本严格等于“绑定”的私聊事件会被接受；首次收到的 `open_id` 使用 DPAPI 保存并回复“绑定成功”。已有绑定时禁止被其他用户覆盖。

## 安全

- Edge 用户目录独立，不存账号密码。
- Windows DPAPI 加密 App Secret 和 open_id。
- `tenant_access_token` 只存在于 `FeishuProvider` 内存缓存。
- 日志和 Excel 不包含任何通知凭证。
- 只有从详情页真实发现 `peerUserId` 时才构造 `https://www.goofish.com/im?...`；否则回退商品 HTTPS URL。
- 飞书卡片只提供真实商品链接或真实提取到的聊天链接；不会自动打开、联系卖家或发起交易。

## 当前验收层级

自动测试覆盖定时规则校验和持久化、飞书配置/绑定引导、定时首轮派发、跨页去重、Excel 超链接和现有单次采集回归。

仍需用户在真实账号上完成：飞书后台配置和绑定、手机实收测试、一次定时采集、登录失效恢复、电脑休眠恢复，以及 WPS 手工打开导出文件。
